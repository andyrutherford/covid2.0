import React from 'react';

import Container from '../UI/Container';

const Country = () => {
  return (
    <Container color='black' bg='orange' width={1}>
      Blue Container
    </Container>
  );
};

export default Country;
